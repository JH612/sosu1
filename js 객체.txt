객체는 key와 value가 나뉘어져있음
js의 객체 안에서는 문자열과 정수 등 자료형이 달라도 같이 존재할 수 있음

객체 생성의 두가지 방법
1. new 사용해서 객체 생성 ex. let user = new Object();
2. {}사용해서 객체 생성 가능 ex. let user = {};

객체의 key는 문자형
value는 모든 자료형 허용

